<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cn.ifafu.ifafu">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_LOGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name="cn.ifafu.ifafu.app.IFAFU"
        android:allowBackup="true"
        android:icon="@drawable/drawable_ifafu"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/drawable_ifafu2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,ExportedService,GoogleAppIndexingWarning,UnusedAttribute">
        <activity android:name=".mvp.other.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.main.MainActivity" />
        <activity android:name=".mvp.login.LoginActivity" />
        <activity android:name=".mvp.syllabus.SyllabusActivity" />
        <activity android:name=".mvp.add_course.AddCourseActivity" />
        <activity android:name=".mvp.exam.ExamActivity" />
        <activity android:name=".mvp.add_exam.AddExamActivity" />
        <activity android:name=".mvp.other.AboutActivity" />

        <!-- 配置APP渠道号 -->
        <meta-data
            android:name="BUGLY_APP_CHANNEL"
            android:value="QQGroup" />

    </application>

</manifest>